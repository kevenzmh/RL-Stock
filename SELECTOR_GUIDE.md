# 简化版选股系统 - 使用指南

## 🎯 快速开始

### 1. 系统测试

首先运行测试，确保所有模块正常工作：

```bash
python test_selector.py
```

**预期输出**：
- ✓ 实时数据获取成功
- ✓ 模型推理成功
- ✓ 小规模选股测试通过

---

### 2. 开始选股

#### 基础用法（选出Top 10）

```bash
python simple_selector.py
```

**执行流程**：
1. 获取所有A股列表
2. 随机抽取100只作为候选池
3. 获取每只股票最近60天数据
4. AI模型预测和评分
5. 输出Top 10股票

**输出示例**：
```
排名    股票代码      股票名称        得分      预期收益    推荐等级
------------------------------------------------------------------------
1      sh.600036    招商银行        75.23     8.50%      🔥 强烈推荐
2      sz.300750    宁德时代        68.91     6.20%      ⭐ 推荐
3      sh.600519    贵州茅台        65.44     5.10%      ⭐ 推荐
...
```

**同时生成CSV文件**：
- `stock_selection_20260122_143025.csv`

---

### 3. 高级用法

#### 选出Top 20

```bash
python simple_selector.py --top 20
```

#### 从更大的股票池中筛选（200只）

```bash
python simple_selector.py --pool 200
```

#### 提高分数要求（只要50分以上）

```bash
python simple_selector.py --min-score 50
```

#### 使用更多历史数据（90天）

```bash
python simple_selector.py --days 90
```

#### 保存到指定文件

```bash
python simple_selector.py -o my_stocks.csv
```

#### 组合使用

```bash
python simple_selector.py --top 30 --pool 300 --min-score 40 --days 90 -o best_picks.csv
```

---

## 📊 输出说明

### CSV文件列说明

| 列名 | 说明 |
|------|------|
| stock_code | 股票代码（如 sh.600036） |
| stock_name | 股票名称 |
| score | 综合得分（0-100） |
| total_return | 预期收益率（%） |
| total_profit | 预期利润（元，基于10000元） |
| sharpe_ratio | 夏普比率（风险调整后收益） |
| recommendation | 推荐等级 |
| max_profit | 最大盈利 |
| min_profit | 最大回撤 |
| volatility | 波动率 |

### 推荐等级

| 等级 | 条件 | 符号 |
|------|------|------|
| 强烈推荐 | 得分≥70 且 收益>5% | 🔥 |
| 推荐 | 得分≥50 且 收益>0 | ⭐ |
| 中性 | 得分≥30 | ➖ |
| 不推荐 | 得分<30 | ❌ |

---

## 🔧 参数说明

### 完整参数列表

```bash
python simple_selector.py [OPTIONS]
```

**可选参数**：

| 参数 | 简写 | 默认值 | 说明 |
|------|------|--------|------|
| --top | -t | 10 | 返回前N只股票 |
| --pool | -p | 100 | 候选股票池大小 |
| --min-score | -s | 30 | 最低分数要求 |
| --days | -d | 60 | 使用最近N天数据 |
| --output | -o | 自动 | 输出文件路径 |
| --help | -h | - | 显示帮助信息 |

---

## 💡 使用建议

### 1. 日常使用流程

**每天收盘后**：
```bash
# 快速筛选Top 10
python simple_selector.py

# 查看结果，做进一步研究
```

**周末深度分析**：
```bash
# 从更大范围筛选，提高标准
python simple_selector.py --top 20 --pool 300 --min-score 50 --days 90
```

### 2. 参数选择建议

#### 候选池大小（--pool）
- **100只**：快速筛选（5-10分钟）
- **200只**：平衡（10-20分钟）
- **300只**：全面筛选（20-30分钟）
- **500+**：深度挖掘（30分钟以上）

#### 数据天数（--days）
- **30天**：短期交易
- **60天**：中短期（推荐）
- **90天**：中期
- **120天+**：长期投资

#### 分数要求（--min-score）
- **30分**：广泛筛选
- **40分**：标准要求
- **50分**：优质股票
- **60分+**：严格筛选

### 3. 结果解读

#### 高分股票（70+）
- ✅ 模型认为有较好机会
- ⚠️ 仍需人工研判基本面
- ⚠️ 注意市场环境

#### 中等分数（50-70）
- 可以关注
- 需要更多研究
- 考虑分散投资

#### 低分股票（<50）
- 谨慎对待
- 可能不适合当前时机
- 或者模型未充分学习该类型

---

## ⚠️ 重要提醒

### 免责声明

> **本系统输出结果仅供参考，不构成投资建议**

1. ❌ **不要盲目跟随**
   - AI模型基于历史数据
   - 过去表现≠未来结果
   - 需要人工判断

2. ✅ **正确使用方式**
   - 作为初筛工具
   - 缩小研究范围
   - 结合基本面分析

3. ⚠️ **风险控制**
   - 分散投资
   - 设置止损
   - 控制仓位
   - 理性决策

### 建议的完整投研流程

```
AI选股（本系统）
    ↓
基本面研究
    ↓
技术面确认
    ↓
估值分析
    ↓
风险评估
    ↓
投资决策
```

---

## 🐛 故障排查

### 常见问题

#### 1. "模型文件不存在"

**解决方案**：
```bash
# 检查models目录
ls models/

# 应该看到以下文件之一：
# - ppo2_enhanced_100000.pkl
# - ppo2_stock_100000.pkl
# - quick_test_model.zip
```

#### 2. "Baostock连接失败"

**解决方案**：
```bash
# 检查网络连接
# 尝试重新运行
```

#### 3. "获取数据失败"

**可能原因**：
- 网络问题
- 股票代码错误
- 该股票停牌

**解决方案**：
- 检查网络
- 重新运行

#### 4. "内存不足"

**解决方案**：
```bash
# 减少候选池大小
python simple_selector.py --pool 50

# 减少返回数量
python simple_selector.py --top 5
```

---

## 📈 性能优化

### 运行时间参考

| 配置 | 预计时间 | 适用场景 |
|------|---------|---------|
| pool=50 | 3-5分钟 | 快速测试 |
| pool=100（默认） | 5-10分钟 | 日常使用 |
| pool=200 | 10-20分钟 | 周末分析 |
| pool=500 | 30-50分钟 | 深度挖掘 |

### 加速技巧

1. **使用GPU**（如果有）
   - 模型会自动使用GPU加速

2. **减少数据天数**
   ```bash
   python simple_selector.py --days 30
   ```

3. **分批处理**
   ```bash
   # 第一批
   python simple_selector.py --pool 100 -o batch1.csv
   
   # 第二批  
   python simple_selector.py --pool 100 -o batch2.csv
   
   # 合并结果
   ```

---

## 🔄 下一步

### 进阶功能（可选）

1. **定时任务**
   - 设置每天自动运行
   - 邮件发送结果

2. **Web界面**
   - 可视化展示
   - 在线查看历史

3. **投资组合管理**
   - 跟踪持仓
   - 收益分析

如需实现这些功能，请参考：
- `ONLINE_READINESS_REPORT.md` - 系统化实施方案
- `UPGRADE_ROADMAP.md` - 完整升级路线

---

## 📞 技术支持

### 相关文档

- `FEASIBILITY_REPORT.md` - 项目可行性分析
- `IMPROVEMENTS_SUMMARY.md` - 已完成的改进
- `ENHANCEMENTS.md` - 增强功能详解
- `ONLINE_READINESS_REPORT.md` - 线上准备度评估

### 反馈问题

如遇到问题，请提供：
1. 完整的错误信息
2. 运行的命令
3. 系统环境信息

---

## 📝 更新日志

### v1.0 (2026-01-22)

**新增功能**：
- ✅ 实时数据获取（realtime_data.py）
- ✅ 模型推理引擎（model_inference.py）
- ✅ 简化选股脚本（simple_selector.py）
- ✅ 快速测试脚本（test_selector.py）

**特性**：
- 支持批量预测
- 自动评分排序
- CSV结果输出
- 命令行参数配置

---

**祝投资顺利！** 📈💰

**记住：股市有风险，投资需谨慎！**
